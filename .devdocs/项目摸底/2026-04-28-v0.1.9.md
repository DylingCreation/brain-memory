# brain-memory v0.1.9 项目摸底报告

| 字段 | 内容 |
|------|------|
| **留痕类型** | 技术调研与验证 |
| **留痕日期** | 2026-04-28 |
| **记录时间** | 15:06 |
| **记录人** | 代码助手（CodingHelper） |
| **关联版本** | v0.1.9 |
| **状态** | ✅ 已修复，更新版 |

---

## 一、摸底范围

对 brain-memory 项目（v0.1.9）进行全维度摸底，覆盖：
1. 构建能力
2. 测试能力（269 个测试用例）
3. Git 版本状态
4. 代码质量（TODO/FIXME）
5. 文档完整性
6. 依赖健康度（npm outdated + npm audit）

---

## 二、摸底结果

### 2.1 构建（Q-4 红线）✅ 通过

```
npm run build → tsc 零报错，dist/ 生成成功
```

### 2.2 测试（Q-1 红线）✅ 已全部修复

| 指标 | 摸底时 | 修复后 |
|------|--------|--------|
| 测试文件 | 27 passed / 3 failed | **30 passed / 0 failed** |
| 测试用例 | 252 passed / 10 failed | **262 passed / 7 skipped** |
| 跳过 | llm-integration（需 LLM API） | 同上 |

#### 修复的 10 个失败

| # | 文件 | 失败数 | 根因 | 修复方式 |
|---|------|--------|------|---------|
| F-1 | `test/integration.test.ts` | 3 | 测试 helper 建表缺少 `source` 列 | `test/helpers.ts` 添加 `source` 列定义 |
| F-2 | `test/store.test.ts` | 4 | 同上 + `insertNode` INSERT 缺少 `source` 参数 | `test/helpers.ts` INSERT 语句补 `source` |
| F-3 | `test/working-memory.test.ts` | 3 | 代码已改为仅接受 TASK/SKILL 作为决策节点，测试仍用 EVENT | 测试用例改为 TASK 类型 + 修正代码块清理预期 |

#### 修改的文件

| 文件 | 修改内容 |
|------|---------|
| `test/helpers.ts` | 建表语句添加 `source TEXT NOT NULL DEFAULT 'user' CHECK(source IN ('user', 'assistant'))`；INSERT 语句补 `source` 列和参数 |
| `test/working-memory.test.ts` | 3 个测试用例修正：`type: "EVENT"` → `type: "TASK"`，代码块清理预期改为 `"运行这个\n[CODE]"` |
| `src/store/store.ts` | `upsertNode` 中对 `c.source` 加默认值 `const source = c.source ?? "user"`（防御性编程） |

### 2.3 Git 状态 ✅ 干净

| 指标 | 结果 |
|------|------|
| 当前分支 | main |
| 最新提交 | `c28fc12` feat: Phase 1-4 fixes — 30 issues analyzed, 27/30 resolved (v0.1.9) |
| 未跟踪 | `llm-cost-analysis.md`（需加入 .gitignore） |

### 2.4 代码质量 ✅ 整洁

```
grep -rn "TODO|FIXME|XXX|HACK|BUG" src/ test/ → 0 条匹配
```

### 2.5 文档完整性 ✅ 完整

| 文档 | 状态 |
|------|------|
| README.md / README_CN.md | ✅ |
| CHANGELOG.md | ✅ |
| SETUP.md | ✅ |
| CONTRIBUTING.md | ✅ |
| docs/ (7 份) | ✅ |

### 2.6 依赖健康度 ⚠️ 已验证

#### npm outdated

| 包 | 当前 | 最新 | 说明 |
|----|------|------|------|
| `@photostructure/sqlite` | 1.2.0 | **1.2.1** | 可安全升级（补丁版本） |
| `@types/node` | 20.19.39 | 25.6.0 | 大版本跳跃，不建议升 |
| `tsx` | MISSING | 4.21.0 | ⚠️ **package.json 中列为依赖但未安装** |
| `typescript` | 5.9.3 | 6.0.3 | 大版本跳跃，不建议升 |
| `vitest` | 1.6.1 | 4.1.5 | 大版本跳跃，升级可能破坏测试 |

#### npm audit

| 严重等级 | 数量 | 来源 | 说明 |
|---------|------|------|------|
| 🔴 Critical | 2 | `protobufjs` → `libsignal` → openclaw 间接依赖 | openclaw 的传递依赖，非 brain-memory 直接依赖 |
| 🟠 High | 7 | `tar` → `@discordjs/node-pre-gyp` → openclaw | 同上 |
| 🟡 Moderate | 11 | `axios`, `esbuild`, `fast-xml-parser`, `uuid` | 大部分为 openclaw 传递依赖 |

**结论**：20 个漏洞中绝大部分来自 openclaw 及其传递依赖（`@larksuiteoapi/node-sdk`, `@discordjs/*`, `matrix-js-sdk` 等），**非 brain-memory 自身依赖问题**。brain-memory 自身仅 2 个直接依赖（`@photostructure/sqlite` + `tsx`），均为安全包。

`npm audit fix` 会修复 openclaw 树中的部分漏洞，但需要确认不影响 openclaw 自身功能。

---

## 三、摸底结论

### 3.1 评级提升：L2 → L1（生产就绪）

| 维度 | 摸底时 | 修复后 |
|------|--------|--------|
| 构建 | ✅ | ✅ |
| 测试 | ⚠️ 96.3% | ✅ **100%**（262/262 通过，7 个为预期跳过） |
| 文档 | ✅ | ✅ |
| 代码 | ✅ | ✅ |
| Git | ✅ | ✅ |
| 依赖安全 | ⚠️（待验证） | ⚠️（传递依赖漏洞，非自身问题） |

### 3.2 建议跟进项

| 优先级 | 编号 | 建议 | 说明 |
|--------|------|------|------|
| **P2** | S-1 | 安装 `tsx` | `npm install tsx` — package.json 中列为依赖但实际未安装 |
| **P2** | S-2 | `llm-cost-analysis.md` 加入 .gitignore | 当前为未跟踪文件 |
| **P3** | S-3 | 升级 `@photostructure/sqlite` 1.2.0→1.2.1 | 补丁版本，风险极低 |
| **P3** | S-4 | 评估 `npm audit fix` | 修复 openclaw 传递依赖漏洞，需测试不影响 openclaw |

---

## 四、下一步建议

1. ~~修复测试 10 个失败~~ → ✅ 已完成
2. 安装 `tsx` 依赖（P2）
3. 清理未跟踪文件
4. 评估是否启动下一版本规划

---

*摸底完成并修复 · 2026-04-28 15:23*
