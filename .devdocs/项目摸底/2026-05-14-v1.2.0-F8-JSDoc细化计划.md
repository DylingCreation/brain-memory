# F-8 JSDoc 补全 — 细化实施计划（5 批次）

| 字段 | 内容 |
|------|------|
| **日期** | 2026-05-14 |
| **记录人** | 代码助手 |
| **关联版本** | v1.2.0 |
| **目标** | 12.7% → 80%（需补 148 个导出项） |

---

## 总览

| 指标 | 数值 |
|------|------|
| 总导出数 | 220 |
| 已有 JSDoc | 28 |
| 需补 JSDoc | ~158 |
| 目标覆盖率 | 80%（176/220） |

---

## 5 批次计划

### 批次依赖关系

```
① 类型定义层 (独立)
② 核心 API 层 (依赖 ①)
③ 存储层 (依赖 ①)
④ 算法层 (依赖 ②③)
⑤ 辅助模块 (依赖 ①)
```

---

## 批次 ①：类型定义层（32 导出，已有 3，需补 29）

**目标文件**：`src/types.ts`

| 导出项 | 说明 |
|--------|------|
| MEMORY_CATEGORIES (const) | 8 类记忆分类常量 |
| MemoryCategory (type) | 8 类记忆联合类型 |
| GraphNodeType (type) | 图节点类型 |
| EdgeType (type) | 11 种边类型 |
| EDGE_FROM_CONSTRAINT | from 端类型约束 |
| EDGE_TO_CONSTRAINT | to 端类型约束 |
| NodeStatus (type) | active / deprecated |
| BmNode (interface) | 节点完整定义 |
| BmEdge (interface) | 边完整定义 |
| WorkingMemoryConfig | 工作记忆配置 |
| WorkingMemoryState | 工作记忆状态 |
| FusionConfig | 知识融合配置 |
| ReasoningConfig | 推理检索配置 |
| MemoryInjectionConfig | 记忆注入格式配置 |
| MemorySharingConfig | 多 Agent 记忆共享配置 |
| SharingMode (type) | 共享模式枚举 |
| ReflectionKind (type) | 反思类型 |
| ReflectionInsightType (type) | 洞察类型 |
| ReflectionInsight | 反思洞察结构 |
| ReflectionResult | 反思结果 |
| ExtractionResult | 提取结果 |
| FinalizeResult | 最终化结果 |
| RecallResult | 召回结果 |
| EmbeddingConfig | Embedding 配置 |
| DecayConfig | 衰减配置 |
| NoiseFilterConfig | 噪声过滤配置 |
| RerankConfig | 重排序配置 |
| ReflectionConfig | 反思系统配置 |
| BmConfig | 主配置接口 |
| DEFAULT_CONFIG | 默认配置实例 |

**预估**：~2 小时

---

## 批次 ②：核心 API 层（32 导出，已有 0，需补 32）

**目标文件**：

| 文件 | 导出项 | 说明 |
|------|--------|------|
| `engine/context.ts` | ContextEngine + 7 方法 + 7 类型 + createContextEngine | 统一上下文引擎 |
| `engine/embed.ts` | EmbedFn + BatchEmbedFn + createEmbedFn + createBatchEmbedFn + EmbedCacheStats + getEmbedCacheStats + clearEmbedCache | Embedding 客户端 |
| `engine/llm.ts` | CompleteFn + LlmConfig + createCompleteFn | LLM 客户端 |
| `plugin/hooks.ts` | 12 Hook 类型 + createHookRegistry | 开发者 Hook 系统 |

**预估**：~2 小时

---

## 批次 ③：存储层（44 导出，已有 1，需补 43）

**目标文件**：

| 文件 | 导出项 | 说明 |
|------|--------|------|
| `store/store.ts` | 34 函数 + 1 类型 | CRUD + 搜索 + 向量 + 社区 + 消息 |
| `store/adapter.ts` | IStorageAdapter 接口 + 10 辅助类型 | 存储抽象接口 |
| `store/sqlite-adapter.ts` | SQLiteStorageAdapter 类 | SQLite 实现 |

**预估**：~2.5 小时

---

## 批次 ④：算法层（38 导出，已有 2，需补 36）

**目标文件**：

| 文件 | 导出项 | 说明 |
|------|--------|------|
| `graph/pagerank.ts` | 7 导出（已有 1） | 全局/增量 PageRank |
| `graph/community.ts` | 6 导出（已有 1） | 全量/增量社区检测 |
| `graph/dedup.ts` | 3 导出 | 向量去重 |
| `graph/maintenance.ts` | 1 导出（已有 1） | 维护流程 + 智能触发 |
| `recaller/recall.ts` | 1 导出 | Recaller 类 |
| `retriever/vector-recall.ts` | 2 导出 | 向量召回 |
| `retriever/hybrid-recall.ts` | 2 导出 | 混合召回 |
| `retriever/admission-control.ts` | 4 导出 | 准入控制 |
| `retriever/intent-analyzer.ts` | 3 导出 | 意图分析 |
| `retriever/query-expander.ts` | 1 导出 | 查询扩展 |
| `retriever/reranker.ts` | 4 导出 | 重排序 |
| `fusion/analyzer.ts` | 7 导出 | 知识融合 |

**预估**：~2.5 小时

---

## 批次 ⑤：辅助模块（12 导出，需补 12）

**目标文件**：

| 文件 | 导出项 | 说明 |
|------|--------|------|
| `reflection/extractor.ts` | 1 | 反思提取 |
| `reflection/store.ts` | 3 | 反思存储 |
| `reasoning/engine.ts` | 5 | 推理引擎 |
| `format/assemble.ts` | 2 | 记忆组装 |
| `noise/filter.ts` | 1 | 噪声过滤 |

**已覆盖无需补**：
- `extractor/heuristic.ts` (66.7%) ✅
- `utils/json.ts` (66.7%) ✅
- `utils/truncate.ts` (66.7%) ✅
- `decay/presets.ts` (60%) ✅
- `scope/isolation.ts` (50%) ✅
- `working-memory/manager.ts` (33.3%) ✅
- `session/compressor.ts` (50%) ✅
- 100% 覆盖的 4 个文件 ✅

**预估**：~1.5 小时

---

## 总预估

| 批次 | 需补导出 | 预估时间 |
|------|---------|---------|
| ① 类型定义层 | 29 | ~2h |
| ② 核心 API 层 | 32 | ~2h |
| ③ 存储层 | 43 | ~2.5h |
| ④ 算法层 | 36 | ~2.5h |
| ⑤ 辅助模块 | 12 | ~1.5h |
| **合计** | **~152** | **~10.5 小时（2 工作日）** |

## 验收标准

| 指标 | 目标 | 验证方式 |
|------|------|---------|
| 整体覆盖率 | ≥ 80% | `scripts/scan-jsdoc-coverage.js` |
| 公共 API 覆盖率 | 100% | 人工确认 ①② 批次 |
| typedoc 生成无错误 | ✅ | `npm run docs` |
| build 零报错 | ✅ | `npm run build` |

---

*本计划由代码助手根据《工程协作手册》§2.2 模板生成 · 2026-05-14*
