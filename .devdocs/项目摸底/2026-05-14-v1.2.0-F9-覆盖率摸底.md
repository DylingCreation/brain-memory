# F-9 覆盖率补全 — 摸底 + 实施计划

| 字段 | 内容 |
|------|------|
| **日期** | 2026-05-14 |
| **记录人** | 代码助手 |
| **关联版本** | v1.2.0 |
| **来源** | v1.0.0 复盘 P2-2 |

---

## 一、摸底数据

v1.0.0 复盘记录 5 个模块覆盖率 < 80%：

| 模块 | v1.0.0 复盘 | 说明 |
|------|-----------|------|
| `src/fusion/analyzer.ts` | 61.5% | 知识融合：LLM 决策路径 + executeFusion 未覆盖 |
| `src/session/compressor.ts` | 62.2% | 会话压缩：仅 3 个测试 |
| `src/utils/json.ts` | 62.7% | JSON 工具：异常路径 + nested 场景未覆盖 |
| `src/reasoning/engine.ts` | 69.2% | 推理引擎：核心逻辑未覆盖 |
| `src/graph/community.ts` | 73.5% | 社区检测：增量路径 + summarize 未覆盖 |

---

## 二、逐模块分析

### 2.1 fusion/analyzer.ts（61.5%）

现有测试：`test/fusion-analyzer.test.ts`（部分）

未覆盖路径：
- `shouldRunFusion()` — 阈值判断
- `executeFusion()` — link 路径
- `decideFusion()` — LLM 决策路径
- `runFusion()` — 降级路径（无 LLM 时）

### 2.2 session/compressor.ts（62.2%）

现有测试：`test/compressor.test.ts`（3 用例）

未覆盖路径：
- 大量 session 压缩分支
- boundary 条件

### 2.3 utils/json.ts（62.7%）

现有测试：部分

未覆盖路径：
- `safeJsonParse()` 异常分支
- nested JSON 处理
- edge cases

### 2.4 reasoning/engine.ts（69.2%）

现有测试：`test/reasoning.test.ts`（14 用例，控制析逻辑）

未覆盖路径：
- `runReasoning()` 核心逻辑
- prompt 构建

### 2.5 graph/community.ts（73.5%）

现有测试：部分

未覆盖路径：
- `runIncrementalCommunities()` 增量路径
- `summarizeCommunities()` LLM 调用
- boundary 条件

---

## 三、实施计划（按难度排序）

### 批次 ①：简单工具模块（0.5 天）

| 模块 | 需补用例 | 说明 |
|------|---------|------|
| `utils/json.ts` | ~5 | 异常路径 + edge cases |
| `session/compressor.ts` | ~5 | boundary 条件 |

### 批次 ②：算法模块（1 天）

| 模块 | 需补用例 | 说明 |
|------|---------|------|
| `fusion/analyzer.ts` | ~10 | shouldRunFusion + executeFusion + 降级路径 |
| `graph/community.ts` | ~8 | 增量 LPA + summarize |
| `reasoning/engine.ts` | ~5 | runReasoning 核心路径 |

### 预估

| 批次 | 用例 | 时间 |
|------|------|------|
| ① 工具模块 | ~10 | 0.5 天 |
| ② 算法模块 | ~23 | 1 天 |
| **合计** | **~33** | **1.5 天** |

---

## 四、验收标准

| 指标 | 目标 |
|------|------|
| 5 个模块覆盖率 | 全部 ≥ 80% |
| 整体覆盖率 | 不劣于 71.88% |
| `npm test` | 0 failed |

---

*本报告由代码助手生成 · 2026-05-14*
