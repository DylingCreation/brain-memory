# v0.2.0 兼容性检查记录

| 字段 | 内容 |
|------|------|
| **留痕类型** | 质量检查记录 |
| **检查日期** | 2026-04-29 |
| **检查人** | 代码助手（CodingHelper） |
| **关联版本** | v0.2.0 |
| **对应规划** | v0.2.0 发布检查清单 — 兼容性检查 |

---

## 一、API 兼容性检查

### 检查方法
核对 `index.ts` 全部导出签名 + `ContextEngine` 构造函数 + 全部公共方法。

### 检查结果

| 检查项 | 结果 | 说明 |
|--------|------|------|
| ContextEngine 构造函数 | ✅ 无破坏性变更 | 签名仍为 `constructor(config: BmConfig)` |
| processTurn | ✅ 无破坏性变更 | 参数和返回值完全兼容 |
| recall | ✅ 无破坏性变更 | 签名不变 |
| onTurnEnd / onSessionEnd | ✅ 无破坏性变更 | 签名不变 |
| 新增 healthCheck() | ✅ 新增方法 | 纯加法，不影响现有调用 |
| getStats() 增强 | ✅ 向后兼容 | 顶层字段保留，新增字段为嵌套结构 |
| 其他全部导出 | ✅ 无破坏性变更 | Recaller、Extractor、assembleContext 等签名不变 |

---

## 二、配置兼容性检查

### 检查方法
核对 `BmConfig` 接口 + `DEFAULT_CONFIG` 默认值 + 环境变量。

### 检查结果

| 检查项 | 结果 | 说明 |
|--------|------|------|
| BmConfig 接口 | ✅ 无破坏性变更 | 未删除/重命名字段，未新增必填字段 |
| DEFAULT_CONFIG | ✅ 无破坏性变更 | 旧字段保留且默认值不变，新增字段均有默认值 |
| 环境变量 | ✅ 兼容 | `BM_LOG_LEVEL` 新增；`BM_LOG_LLM` 仍有效；`BM_DEBUG` 功能合并到 `BM_LOG_LEVEL=debug` 但不破坏旧行为 |
| LLM/Embedding 配置 | ✅ 无破坏性变更 | 空配置时从「抛异常」变为「优雅降级跳过」，对已配置用户无影响 |
| Schema 变更 | ✅ 无破坏性变更 | 仅新增 `bm_meta` 表，未修改现有表结构 |

---

## 三、结论

**✅ 兼容性检查通过。** v0.2.0 全部变更为**向后兼容**的纯加法（新增方法、新增字段、新增配置项均有默认值），无任何破坏性变更。

---

*检查完成 · 2026-04-29*
