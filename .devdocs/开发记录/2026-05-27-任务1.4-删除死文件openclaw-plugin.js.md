# 任务 1.4：删除死文件 `openclaw-plugin.js`

**来源**: M2-P0-1  
**执行日期**: 2026-05-25  
**执行人**: AI  
**状态**: ✅ 已完成

---

## 问题描述

`openclaw-plugin.js`（根目录，4,837 字节）是 v1.0 旧版插件入口，第 3 行 import `BrainMemoryHandler` from `./src/plugin/handler.js`，但该文件**不存在**。若被运行时加载将立即抛出 `ERR_MODULE_NOT_FOUND`。

## 执行内容

### 1. 引用检查

```bash
grep -r "openclaw-plugin.js" . --include="*.ts" --include="*.js" --include="*.json"
```

**结果**：无 TypeScript/JavaScript 源文件引用。仅在以下位置出现：

| 文件 | 类型 | 说明 |
|------|------|------|
| `.devdocs/版本规划/v1.0.0.md` | 文档 | 历史记录 |
| `.devdocs/版本复盘/2026-05-25-v1.8.0.md` | 文档 | 历史记录 |
| `dist/openclaw-plugin.js` | 编译产物 | sourceMappingURL 引用（非源代码） |
| `.npmignore` | 配置 | 已排除出 npm 发布 |

### 2. 删除操作

```bash
rm /home/yangchen/.openclaw/codinghelper/brain-memory/openclaw-plugin.js
```

### 3. 验证

```bash
# 确认根目录文件已删除
ls openclaw-plugin.js → 没有那个文件或目录

# 确认 dist/ 中的编译产物未受影响
ls dist/openclaw-plugin.js → dist/openclaw-plugin.js ✅
```

---

## 验证结论

✅ `openclaw-plugin.js` 已删除。`dist/` 中的编译产物（由 `openclaw-plugin.ts` 编译而来）未受影响，用户的插件加载路径（通过 `dist/` 或 `openclaw-register.ts`）保持正常。

---

## 影响分析

- **风险**：无。该文件从未被任何代码引用，`.npmignore` 已排除发布
- **副作用**：无。`dist/openclaw-plugin.js` 正常保留
