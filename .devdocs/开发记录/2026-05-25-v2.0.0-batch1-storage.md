# 批次 1 — 存储层重构 — 开发记录

| 字段 | 内容 |
|------|------|
| **日期** | 2026-05-25 |
| **记录人** | OpenClaw CodingHelper |
| **关联版本** | v2.0.0 |
| **分支** | `v2.0.0-dev` |

## 一、完成清单

| 子任务 | 状态 | Commit |
|--------|------|--------|
| **1a: S-5 God Object 拆分** | ✅ | `7a5d3fe` |
| **1b: S-6 强制转换消除** | ✅ | `4c1aa44` |
| **1c: S-2 ISearchIndex 接口 + LanceDB 实现** | ✅ | `4b3f5e0` |
| **1d: getDb() 彻底删除** | ✅ | `b3dea03` |

## 二、1a: God Object 拆分

store.ts 595 行 → 7 子模块 + barrel:

```
store/
├── store.ts                  ← barrel re-export (64 行)
├── storage/
│   ├── _helpers.ts           ← 共享: uid/toNode/toEdge/normalizeName/SqlRow
│   ├── nodes.ts              ← 节点 CRUD (7 函数)
│   ├── edges.ts              ← 边 CRUD (3 函数 + allEdges)
│   ├── search.ts             ← FTS5 + 向量搜索 + topNodes
│   ├── vectors.ts            ← 向量操作 (4 函数)
│   ├── communities.ts        ← 社区 CRUD (4 函数)
│   ├── messages.ts           ← 消息 CRUD (4 函数)
│   └── graph-walk.ts         ← 图遍历 + 社区向量搜索
├── search/                   ← 伴随语义索引 (S-2 新增)
│   ├── index.ts              ← ISearchIndex 接口 (7 方法)
│   └── lancedb.ts            ← LanceDBSearchIndex 实现 (~180 行)
```

**策略**: barrel pattern — store.ts 保持统一入口，所有消费者 import 路径不变。

## 三、1b: 强制转换消除 + 1d: getDb() 删除

消除 `(this.storage as SQLiteStorageAdapter)` 强转 + `getDb()` 方法：

| 文件 | 变更 |
|------|------|
| context.ts | 删除 `getDb()` 方法；`getStats()` 12行 raw SQL → stats.byType/byTemporalType/bySource |
| adapter.ts | StorageStats 新增 3 类分项统计 |
| sqlite-adapter.ts | 填充新统计字段 |
| lancedb-adapter.ts | 补齐兼容默认值 |
| assemble.ts | `db: DatabaseSyncInstance` → `storage: IStorageAdapter`；`getEpisodicMessages()` → adapter 方法 |
| plugin/core.ts | `engine.getDb()` → `engine.getStorage()` |

## 四、1c: ISearchIndex

- **接口**: 7 方法 (indexNode / indexNodes / removeNode / semanticSearch / rebuild / count / close)
- **实现**: LanceDBSearchIndex — 独立表 `bm_search_index`，schema 含 node_id + vector + scope 四字段
- **搜索**: post-filter scope（后续优化 pre-filter）
- **重建**: drop → batch embed(50/批) → bulk index

## 五、验证总览

| 项目 | 结果 |
|------|------|
| `npm run build` | ✅ 零报错 |
| `tsc --strictNullChecks --noEmit` | ✅ 零报错 |
| `npm test` | ✅ 778 pass / 2 flake / 0 新增回归 |
| `as SQLiteStorageAdapter` 残留 | ✅ **0 处** |
| `getDb()` 外部调用 | ✅ **0 处** |
| 存储层文件数 | 16 (从 1 个 store.ts) |
