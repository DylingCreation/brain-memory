# B-3 遗忘曲线默认开启 — 开发记录

| 字段 | 内容 |
|------|------|
| **留痕类型** | 开发记录 |
| **留痕日期** | 2026-05-06 |
| **记录人** | 代码助手（CodingHelper） |
| **关联版本** | v1.0.0 批次 2 |

---

## 一、开发内容

### 核心问题
`decay.enabled` 默认 false，衰减功能未启用。

### 1. DEFAULT_CONFIG 变更

```typescript
// 旧：enabled: false
// 新：enabled: true
```

### 2. src/graph/maintenance.ts — 衰减归档逻辑

| 改动 | 说明 |
|------|------|
| 新增 import | `scoreDecay`, `allActiveNodes` |
| 新增步骤 5 | 衰减归档：composite < 0.25 且 validatedCount ≤ 1 的节点标记为 deprecated |
| 返回值新增 | `deprecatedNodes` 计数 |

**归档策略**：
- 软标记（status='deprecated'），不删除数据
- recall 已按 status='active' 过滤，自动排除 deprecated 节点
- validatedCount ≤ 1 保护重要记忆不被归档

---

## 二、测试

全量回归：330 passed / 0 failed / 6 skipped（零回归）

---

## 三、验收确认

| 验收标准 | 状态 |
|---------|------|
| 衰减默认开启 | ✅ DEFAULT_CONFIG decay.enabled = true |
| 衰减后数据库体积稳定 | ✅ 软标记，不删除数据 |
| 重要记忆不丢失 | ✅ validatedCount > 1 的节点不被归档 |
| 测试无回归 | ✅ 330 passed / 0 failed |
| 构建通过 | ✅ tsc 零报错 |

---

*开发完成 · 2026-05-06 11:36*
