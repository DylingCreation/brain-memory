# Phase 3-1：安全加固 + 清理冗余

**来源**: 综合报告 — M1/M2/M3 尾项  
**执行日期**: 2026-05-27  
**执行人**: AI  
**状态**: ✅ 已完成（3 项任务）

---

## 包含任务

| # | 任务 | 来源 | 状态 |
|---|------|------|------|
| 3.5 | .env.example 安全提示 + CI lint/check:versions | M1-P0-1 防复发 | ✅ |
| 3.6 | 删除 toNodeFromRaw 重复定义 | M3-P2-1 | ✅ |
| 3.3 | 合并 openclaw-plugin.ts 桥接层 | M2-A-1 | ✅ |

---

## 任务 3.5：安全提示 + CI 质量门 ✅

### .env.example 安全提示

```diff
+ # ⚠️ SECURITY: Do NOT put real credentials in .env.
+ # Use environment variables or a secrets manager instead.
```

### CI 补充 lint + check:versions

`.github/workflows/test.yml` 新增两个步骤：

```yaml
- name: Check version consistency
  run: npm run check:versions

- name: Lint (0 errors gate)
  run: npm run lint
```

**之前**: CI 只跑 `build + test`，不检查 lint 和版本一致性  
**之后**: CI 先检查版本一致性 → lint 0-error 门控 → build → test

---

## 任务 3.6：删除 toNodeFromRaw 重复定义 ✅

### 问题

`_helpers.ts:toNode()` 和 `sqlite-adapter.ts:toNodeFromRaw()` 是**完全相同的函数**（逐行比对确认），分别维护在 2 个文件中。

### 修复

| 操作 | 文件 |
|------|------|
| 删除 `toNodeFromRaw` 函数 + 注释（28 行） | `src/store/sqlite-adapter.ts` |
| 删除 `typeToCategory` 函数（仅被 toNodeFromRaw 使用，7 行） | `src/store/sqlite-adapter.ts` |
| 新增 `import { toNode } from './storage/_helpers'` | `src/store/sqlite-adapter.ts` |
| 替换 `toNodeFromRaw` → `toNode` 的 1 处调用 | `src/store/sqlite-adapter.ts` |
| 清理 2 个未使用的类型导入（GraphNodeType, NodeStatus） | `src/store/sqlite-adapter.ts` |

**净删除：~42 行**

---

## 任务 3.3：合并 openclaw-plugin.ts 桥接层 ✅

### 问题

`openclaw-plugin.ts`（11 行）是一个纯 re-export 文件，仅将 `src/plugin/core.ts` 的导出转发一次。导致 5 层导入链：
```
index → register → wrapper → plugin → core
```

### 修复

| 操作 | 文件 |
|------|------|
| 修改 import：`'./openclaw-plugin'` → `'./src/plugin/core'` | `openclaw-wrapper.ts` |
| 删除 `git rm openclaw-plugin.ts` | — |
| 移除 tsconfig 引用 | `tsconfig.json` `tsconfig.build.json` `tsconfig.plugin.json` |
| 移除 coverage exclude 引用 | `vitest.config.ts` |
| 移除 lint 脚本引用 | `package.json` |

### 导入链简化

```
之前 (5层): index → register → wrapper → plugin → core
之后 (4层): index → register → wrapper → core
```

---

## 全量验证

| 指标 | 结果 |
|------|------|
| Lint | **0 errors** ✅ |
| Test Files | 66 pass / 6 fail / 3 skip (75) |
| Tests | 834 pass / 7 fail / 35 skip (876) |
| 新增回归 | **0**（失败均为预存 flaky 测试） ✅ |

---

## 代码债务消除

| 债务 | 修复前 | 修复后 |
|------|--------|--------|
| .env.example 安全提示 | 无 | ✅ |
| CI lint 门控 | 无 | ✅ |
| CI 版本检查 | 无 | ✅ |
| toNode 重复定义 | 2 份 | **1 份** (-42行) |
| 导入链层数 | 5 层 | **4 层** (-1层) |
