# 任务 1.7：修复 `core-only.tsconfig.json` 中不存在的文件引用

**来源**: M1-P1-2  
**执行日期**: 2026-05-25  
**执行人**: AI  
**状态**: ✅ 已完成

---

## 问题描述

`core-only.tsconfig.json` 中存在三处问题：

1. **include 中**：`"src/plugin/handler.ts"` — 文件不存在（曾在 v1.0 存在，v2.0 已删除，但 tsconfig 未同步）
2. **exclude 中**：`"src/preferences/slots.ts"` — 从未存在的文件（曾是规划中的功能，已废弃）
3. **过时注释**：`// This references similarity.ts which has errors` — 实际情况是 `similarity.ts` 和 `fusion/analyzer.ts` 都存在且可正常编译

## 执行内容

### 1. 验证所有 include 文件存在性

共检查 23 条 include 条目，结果：
- ✅ 22 个文件存在
- ❌ `src/plugin/handler.ts` — 不存在

### 2. 移除不存在的文件

```diff
- "src/plugin/handler.ts",
```

### 3. 移除从未创建的文件（exclude）

```diff
- "src/preferences/slots.ts",
```

### 4. 移除过时注释

```diff
- "src/fusion/analyzer.ts"  // This references similarity.ts which has errors
+ "src/fusion/analyzer.ts"
```

### 5. 验证

```bash
npx tsc -p core-only.tsconfig.json --noEmit
```

Exit code: 0。tsconfig 中 `"noEmitOnError": false` 的设计意图是容忍非阻塞错误，现有的 TS6307 警告是预存的（import 链中的文件未全部列入 include），与本次修改无关。

---

## 影响分析

- **风险**：无。仅删除了对不存在的文件的引用
- **副作用**：无。不影响现有构建或类型检查流程
- **清理后 include**: 22 个文件（全存在），exclude: 9 条规则

---

## 最终文件状态

```jsonc
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmitOnError": false,
    "skipLibCheck": true,
    "allowImportingTsExtensions": true,
    "emitDeclarationOnly": true
  },
  "include": [
    // 22 个文件 — 全部存在
    "src/types.ts", "src/engine/context.ts", "src/engine/llm.ts",
    "src/engine/embed.ts", "src/store/db.ts", "src/store/store.ts",
    "src/extractor/extract.ts", "src/recaller/recall.ts",
    "src/reflection/extractor.ts", "src/working-memory/manager.ts",
    "src/format/assemble.ts", "src/graph/pagerank.ts",
    "src/graph/community.ts", "src/graph/maintenance.ts",
    "src/decay/engine.ts", "src/noise/filter.ts",
    "src/temporal/classifier.ts", "src/utils/json.ts",
    "src/utils/xml.ts", "src/utils/text.ts",
    "src/scope/isolation.ts", "index.ts"
  ],
  "exclude": [
    // 9 条规则 — 全部有效
    "node_modules", "dist", "tests/**/*", "test/**/*",
    "**/*.test.ts", "**/*.spec.ts", "src/retriever/**/*",
    "src/session/**/*", "src/graph/dedup.ts",
    "src/utils/similarity.ts", "src/fusion/analyzer.ts"
  ]
}
```
