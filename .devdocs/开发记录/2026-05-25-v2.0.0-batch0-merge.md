# 批次 0 — Merge 地基 — 开发记录

| 字段 | 内容 |
|------|------|
| **日期** | 2026-05-25 |
| **记录人** | OpenClaw CodingHelper |
| **关联版本** | v2.0.0 |
| **分支** | `v2.0.0-dev` |

## 一、开发内容

### 0a: v1.8.0 → v2.0.0 地基 merge

将 `new/` 中 Scope 六层升级的 6 个 PR 代码 + Web UI 代码 merge 到 `v2.0.0-dev` 分支（基于 v1.8.0）。

**策略**: 新建 `v2.0.0-dev` 分支 → 逐文件分类处理：

| 分类 | 数量 | 处理方式 |
|------|------|---------|
| v1.8.0 未改动的文件 | 17 | 直接复制 |
| 双方改动文件 | 3 | 手动 merge + re-apply v1.8.0 修复 |

**3 个冲突文件的处理**:

| 文件 | v1.8.0 变更 | new/ 变更 | merge 方式 |
|------|-------------|----------|-----------|
| `src/types.ts` | + maxTokens | + MemoryScopeV2 + BmNode 扩展 | 复制 new/ → 补 maxTokens |
| `src/engine/context.ts` | + getDb() @deprecated | + Scope v2 逻辑 | 复制 new/ → 补 @deprecated |
| `src/plugin/core.ts` | + LLM 健康检查 (F-10) | + Scope v2 + UI Server | 复制 new/ → 补健康检查 + null 修复 |

**新增依赖**: `hono` + `ws` (Web UI server)

**strictNullChecks 修复**: 与 v1.8.0 模式一致 — `pluginInstance!` pragma (4处) + `?? ''` 空值合并 (3处) + `?.` optional chain (1处)

### 0b: K-7 交叉兼容验证

验证 `normalizeHookArgs` 在 merge 后六层 scope 全链路贯通：

```
event.sessionKey → sessionId         ✅
ctx.channelId → platform             ✅
event.senderId → userId              ✅
ctx.conversationId → chatId          ✅
event.threadId → threadId            ✅
api.on() 注册 message_sending        ✅
InternalHookEvent 分支已移除          ✅
```

## 二、验证

| 验证项 | 结果 |
|--------|------|
| `npm run build` | ✅ 零报错 |
| `tsc --strictNullChecks --noEmit` | ✅ 零报错 |
| `npm test` | ✅ 800 cases / 778 pass / 2 flake (预存) / 0 新增回归 (+52 scope v2 tests) |

## 三、变更量

| 指标 | 数据 |
|------|------|
| 变更文件 | 50 |
| 新增行 | +5,459 |
| 删除行 | -825 |
| 新增测试 | +52 (scope-v2 + scope-migration + lancedb-mvp) |
| 新增依赖 | hono + ws |
