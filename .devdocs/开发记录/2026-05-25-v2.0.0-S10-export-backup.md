# S-10 记忆导出/备份 — 开发记录

| 字段 | 内容 |
|------|------|
| **日期** | 2026-05-25 |
| **记录人** | OpenClaw CodingHelper |
| **关联版本** | v2.0.0 |
| **对应规划编号** | S-10 |

## 一、开发内容

**涉及文件**: `src/engine/context.ts` (+110) · `index.ts` (+1)

### ContextEngine.export()

```typescript
engine.export(): MemoryExport
engine.export({ scope: { platform: 'discord' } }): MemoryExport
```

- 导出全量活跃节点 + 边 + 社区摘要
- 可选 `scope` 过滤：六层维度精确匹配
- 输出 `MemoryExport` JSON：version / exportedAt / nodeCount / edgeCount / nodes[] / edges[] / communities[]

### ContextEngine.import()

```typescript
engine.import(data): { imported: number; skipped: number }
```

- 按 `name` 去重（已存在 → skip）
- 边按 `fromId + toId + type` UPSERT
- source 默认 'manual'
- sessionId 统一标记 '__import__'

### 类型定义

| 类型 | 说明 |
|------|------|
| `MemoryExport` | 版本化备份结构 (v2.0.0) |
| `ExportOptions` | scope 过滤选项 |
| `matchExportScope()` | 六层维度匹配辅助 |

## 二、验证

| 验证项 | 结果 |
|--------|------|
| 全量导出 → 1 node / 0 edges | ✅ |
| 导入去重 → 1 skipped | ✅ |
| Scope 过滤 → 0 nodes (platform=discord) | ✅ |
| `npm run build` + strictNullChecks | ✅ |
| `npm test` 全量 | ✅ 0 新增回归 |
