# Performance Tests (Benchmarks)

Benchmark tests for establishing performance baselines.

Tests in this directory are not assertions of correctness — they measure throughput, latency, and memory usage against baselines.
